import styled from "styled-components";
import { StyledLinkColor } from "../../styled-component/StylesColors";

export const StyledList = styled.ul`
    color: ${StyledLinkColor};
`;

export const StyledListItem = styled.li`
    margin: 0 0 10px;
`;
